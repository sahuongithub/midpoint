# never commit credentials
*.env
.env
config/*.local.json
**/secrets*
# large recorded data
# working data stays local; only the built site is published
docs/*
!docs/index.html
!docs/.nojekyll
__pycache__/
*.pyc
.DS_Store
bin/alpaca

# source texts used for research are third-party copyrighted works: never published
research/
# operational logs from scheduled runs
ops/logs/
# local settlement lookups
results/settlement_cache.json
ops/run/
